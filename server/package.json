{
  "name": "loris-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node ./dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p .",
    "reindex": "ts-node scripts/reindexPerfumes.ts",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.4.0",
    "@pinecone-database/pinecone": "^1.0.0",
    "openai": "^4.10.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.3"
  }
}
